# Changelog

## 0.7.0 (2026-08-28)
- 제어 신뢰성 향상(confirm & retry): 명령 전송 후 ~4초 뒤 실제 기기 상태를 다시 읽어 목표에 도달하지 못했으면 자동 재전송(최대 2회). 클라우드→월패드→기기 릴레이 중 명령이 가끔 유실돼 조명이 안 꺼지던 문제 완화. on/off는 절대값이라 재전송해도 안전.
- 제어 시 API 응답을 기다리지 않고 백그라운드에서 전송/확인 → UI는 즉시 반응.

## 0.6.1 (2026-08-28)
- 방문 이력 sensor 제거(0.6.0에서 추가했던 것). 해당 데이터는 현관 카메라가 캡처한 방문 영상 기록으로 매우 드물게 갱신되어(초인종 매번 아님) 실효성이 낮아 롤백. 필요한 핵심 엔티티만 유지.

## 0.6.0 (2026-08-28)
- 방문 이력 sensor 추가: 세대 현관 앞 방문 기록을 표시(state=최근 방문 시각, 속성=방문 횟수·최근 목록). 2분 주기 조회. 지원 단지에서만 생성.

## 0.5.2 (2026-08-28)
- 온수 사용량 단위 수정: MJ → ㎥(물 부피, device_class=water). 월패드/실사용 기준 온수는 물 부피(톤≈㎥)로 계량되며, 기존 MJ 표기는 부정확했음. (전기 kWh, 수도·가스·난방 ㎥는 그대로.)

## 0.5.1 (2026-08-28)
- 기본 폴링 주기 30초 → 15초로 변경. 외부(물리 스위치/앱) 조작을 HA가 더 빨리 반영하도록 새 설치의 기본값을 조정(하한 15초 유지).

## 0.5.0 (2026-08-28)
- 제어 즉시 반응(optimistic-first): 버튼을 누르면 API 응답을 기다리지 않고 UI를 즉시 갱신한 뒤 명령을 보낸다. 스위치/온도/모드가 누르는 즉시 반응한다(실제 기기는 1~3초 뒤 물리적으로 반영).
- 참고: 폴링 주기는 '외부(월패드/앱) 조작' 반영 속도에만 영향을 준다. HA에서의 제어 반응성과는 무관하므로 기본 30초를 권장한다.

## 0.4.2 (2026-08-28)
- 빠른 토글 시 실제 상태와 UI 불일치가 오래 지속되던 문제 완화: 제어 후 명령값 고정 시간을 12초 → 6초로 단축. 월패드 명령 경합(물리 상태 ≠ 마지막 명령)이 발생해도 최대 6초 내에 실제 상태로 정정된다. (경합 자체는 월패드 특성이라 통합에서 방지 불가.)

## 0.4.1 (2026-08-28)
- 제어 직후 순간적으로 "사용할 수 없음"으로 깜빡이던 문제 수정: 폴링 시 이전 기기 상태를 이어받아 일시적 공백에도 기기가 사라지지 않게 하고, 엔티티 available을 마지막 폴링 성공 여부에 묶지 않도록 완화.

## 0.4.0 (2026-08-28)
- 제어 반응 개선: 제어 직후 명령값을 확정(또는 ~12초)까지 고정해, 빠르게 토글해도 폴링의 전환중 값이 상태를 되돌리지 않도록 함(optimistic hold).
- 버그 수정: 에너지 사용량 매핑 오류 교정 — energyType 4=난방(㎥), 5=온수(MJ) (기존에 난방/온수가 뒤바뀌어 있었음).

## 0.3.2 (2026-08-28)
- 로그인 화면 간소화: 언어 선택 제거(한국 아파트 표준 KOR로 고정). 아이디/비밀번호 위주로 정리.

## 0.3.1 (2026-08-28)
- 안전: 최소 폴링 주기를 5초 → 15초로 상향(권장 30~60초). 저장값도 15~600초로 클램프.
- 이유: 공개 배포 특성상 다수 사용자가 과도하게 짧은 주기를 쓰면 직방 서버가 rate-limit/차단해 서비스 전체가 막힐 수 있음. 적응형 폴링이 반응성을 담당하므로 낮은 평상 주기는 불필요.

## 0.3.0 (2026-08-28)
- 적응형 폴링: HA에서 제어 직후 잠깐 빠른 주기(3초×4회)로 재조회 후 평상 주기 복귀 — 연쇄/외부 변화 반영 속도 향상, 평상시 서버 부하는 유지.

## 0.2.1 (2026-08-28)
- 폴링 최적화: 존재하는 기기 타입만 조회(20회마다 전체 재스캔), 에너지/관리비는 30분 주기 캐시.
- 결과: 평소 1회 폴링 요청이 약 15 → 7개로 감소(세대 구성에 따라 다름).

## 0.2.0 (2026-08-28)
- 폴링 주기를 옵션에서 설정 가능(기본 30초, 5~600초). 변경 시 자동 리로드.
- 참고: MHPS는 공식 앱도 기기상태 실시간 스트리밍이 없어 폴링 방식이며, HA에서 직접 제어 시에는 응답으로 즉시 반영(optimistic)된다.

## 0.1.0 (2026-08-28)
- 최초 릴리스.
- MHPS 세대(`isMhpsUser=1`) 지원: 조명(조명/디밍/감성조명), 난방(bl/br), 냉방, 환기, 커튼, 가스밸브, 퇴실스위치, 홈모드, 에너지(전기/수도/가스/온수/난방).
- 레거시 세대(`isMhpsUser=0`) 실험적 지원: 실내환경 센서(온습도/CO2/미세먼지), 스마트콘센트.
- 로그인(SHA-512 비밀번호) + `createDate`/`hashData` 요청 서명, 60초 클라우드 폴링, 계정 잠금 백오프.
